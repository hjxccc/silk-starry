<!-- silk-starry: 子模块导航索引。放在 monorepo 根 CLAUDE.md 里，帮 Claude/人快速路由。 -->
## 子模块导航（改哪个看哪个）

| 子模块 | 用途 | 看哪个文档 |
|---|---|---|
| `<module-a>` | <一句话用途> | `<module-a>/CLAUDE.md` |
| `<module-b>` | <一句话用途> | `<module-b>/CLAUDE.md` |

> 规则：改某个子模块前先读它自己的 CLAUDE.md（就近原则）；跨多个模块的改动先列计划。
> 共享规范（如建表/迁移）见：`<单一来源路径>`，不要在各子模块各写一份。
